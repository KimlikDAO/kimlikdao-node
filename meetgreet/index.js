
/**
 * @param {!Request} req
 * @param {!Environment} env
 * @param {!Context} ctx
 * @param {!URL} url
 * @return {Promise<!Response>}
 */
const yo = (req, env, ctx, url) => {
  url.searchParams.has("challenge")
  return Promise.reject();
}

export default { yo };
